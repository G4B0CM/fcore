[project]
name = "back-end-fcore"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "bcrypt",
    "pytest",
    "dotenv",
    "sqlalchemy",
    "PyJWT",
    "pydantic-settings"
]

[tool.setuptools.packages.find]
where = ["."]